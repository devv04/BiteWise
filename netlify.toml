[build]
  base = "bitewise_frontend/foodscan"
  command = "npm install && npm run build"
  publish = "dist"

# SPA redirect — all routes go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
